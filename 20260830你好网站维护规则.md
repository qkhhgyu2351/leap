# 「你好，丽萍」网站维护规则

> 本文档记录网站的设计规则、布局逻辑和样式规范。维护代码前请先阅读，避免改乱。

---

## 一、颜色规则

| 变量 | 色值 | 用途 |
|---|---|---|
| `--bg` | #F5F7F5 | 页面背景（浅灰绿） |
| `--bg-soft` | #EAF0ED | 柔和背景 |
| `--ink` | #19201E | 主文字（深墨绿黑） |
| `--ink-soft` | #68736F | 次要文字 |
| `--accent` | #C86150 | 砖红 —— 菜单选中、分隔线、hover、"你好" |
| `--accent2` | #47736F | 墨绿 —— 日期、链接、结构文字 |
| `--date-tiffany` | #5B9F98 | 日期色（蒂芙尼绿） |
| `--line` | #DCE5E1 | 边框线 |

---

## 二、字体规则

| 变量 | 字体栈 | 用途 |
|---|---|---|
| `--serif` | Noto Serif SC → Songti SC → 宋体 → Georgia | 标题、正文、金句（衬线） |
| `--sans` | Inter → Noto Sans SC → 苹方 → 微软雅黑 | 日期、菜单、标签、UI（无衬线） |

- 正文基础：16px，行高 1.88，字间距 0.01em
- 文章标题：23px，衬线，行高 1.45
- 首屏副标题：19px，衬线，行高 2

---

## 三、布局规则（最关键）

### 桌面端（≥601px）—— 两套对齐中心

桌面端因为文章卡片有左侧日期栏，所以存在**两套对齐中心**，维护时必须分清：

| 对齐中心 | 适用元素 | 说明 |
|---|---|---|
| **页面中心** | 首屏标题、首屏文字、更多天数、收尾 | 整个浏览器窗口的正中间 |
| **内容区中心** | 文章卡片内容、分隔节点（金句/印章/穿插语） | 偏右 61px，因为左边有 122px 的日期栏 |

**日期栏宽度计算**：104px（日期列）+ 18px（间距）= 122px

**内容区中心偏移计算**：122px ÷ 2 = 61px（内容区中心比页面中心偏右 61px）

#### 桌面端布局示意

```
┌─────────────────────────────────────────┐
│  顶栏（固定，全宽）                      │
├─────────────────────────────────────────┤
│  首屏 Hero（全宽，内容在【页面中心】）    │
│  "你好，丽萍"标题 → 页面中心             │
│  副标题/更新时间 → 页面中心              │
├─────────────────────────────────────────┤
│  信息流（max-width: 820px 居中）         │
│                                         │
│  文章卡片（max-width: 720px 居中）       │
│  ┌──────────┬──────────────────────┐   │
│  │ 日期栏    │  白色卡片内容区       │   │
│  │ 104px    │  从122px开始          │   │
│  │ 右对齐    │  左右内边距各30px     │   │
│  └──────────┴──────────────────────┘   │
│                                         │
│  分隔节点（金句/印章/穿插语）            │
│  → 跟【内容区中心】对齐（偏右61px）      │
│  → 不是页面中心！                        │
│                                         │
│  更多天数/收尾（全宽，内容在【页面中心】）│
└─────────────────────────────────────────┘
```

### 移动端（≤600px）—— 一套对齐中心

移动端没有左侧日期栏，全部单列居中，只有**一套对齐中心：页面中心**。

```
┌──────────────────┐
│  顶栏            │
├──────────────────┤
│  首屏（全宽居中） │
├──────────────────┤
│  信息流          │
│  文章卡片        │
│  ┌────────────┐ │
│  │ 日期（上方）│ │
│  │ 左对齐      │ │
│  ├────────────┤ │
│  │ 白色卡片    │ │
│  │ 内容上28下36│ │
│  │ 左右各20px  │ │
│  └────────────┘ │
│                  │
│  分隔节点（全宽居中）│
└──────────────────┘
```

---

## 四、间距规则

### 桌面端

| 元素 | 间距 |
|---|---|
| 文章卡片之间 | 底部 margin 56px |
| 卡片内标题上方 | 28px |
| 卡片内标题下方 | 15px |
| 卡片内内容左右 | 各 30px |
| 卡片内最后一个元素底部 | 30px |
| 分隔节点上下 padding | 各 28px |
| 分隔节点底部 margin | 56px |
| 分隔内容到上下文章 | 各 84px（56+28） |

### 移动端

| 元素 | 间距 |
|---|---|
| 文章卡片之间 | 底部 margin 36px |
| 卡片内内容上下 | 上 28px、下 36px |
| 卡片内内容左右 | 各 20px |
| `.interlude` 穿插语上下 padding | 各 20px |
| `.interlude` 穿插语底部 margin | 36px |
| `.day-seal`/`.divider` 上下 padding | 各 20px（**已单独适配**，不是继承桌面端） |
| `.day-seal`/`.divider` 底部 margin | 36px（**已单独适配**，不是继承桌面端） |
| 穿插语到上下文章 | 各 56px（36+20） |

---

## 五、组件规则

### 1. 顶栏

- 固定顶部，毛玻璃背景（blur 16px，透明度 92%）
- 左侧：印章 logo + "hello liping" 文字
- 右侧：菜单（今时往日 / 去看更多），hover 有下划线动画
- 当前页菜单用砖红色（--accent）

### 2. 首屏 Hero

- 最小高度 650px
- 背景图：雪野湖清晨雾气，透明度 44%，上下渐隐
- 内容全部**页面正中间**
- 结构：英文 eyebrow → "你好，丽萍"书法标题 → 副标题 → 更新时间胶囊 → 往下慢慢读

### 3. 文章卡片

- 桌面端：左栏日期（104px，右对齐，蒂芙尼绿）+ 右栏白色卡片
- 卡片：圆角 14px，白色半透明背景，轻微阴影
- 标题：衬线 23px，hover 变砖红
- 正文：行高 1.95，段落间距 16px
- 署名：斜体 14px，灰色
- 图片：最大宽度 60%，居中，圆角 4px，透明度 72%，轻微复古滤镜

### 4. 分隔节点（三种）

| 类型 | 样式 | 对齐 |
|---|---|---|
| `.day-seal` 萍印章 | 居中图片，左右短横线 | 内容区中心 |
| `.divider` 金句 | **上下短横线夹一句金句**（结构 `d-line + quote + d-line`，纵向排列，间距 12px）——**没有引号**；金句砖红、衬线 14px | 内容区中心 |
| `.interlude` 穿插语 | 引号 + 大段文字（衬线19px，砖红），滚动淡入 | 内容区中心 |

**重要警告**：分隔节点用 `padding-left: 122px` 实现跟内容区对齐，**绝对不能用 `transform: translateX`**，因为会跟滚动淡入动画的 `transform: translateY` 冲突，导致水平偏移被覆盖。

### 5. 更多天数 / 收尾

- 全宽，内容**页面正中间**（不跟内容区对齐）
- 收尾有背景图（黄昏），上下渐隐
- **注意**：这两个都是**全宽元素**，靠 `text-align:center` 自然落在页面中心，**绝对不要加 `padding-left:122px`**（那会让内容偏右 61px，跟全宽背景图错位）。只有 `.stream` 内部的分隔节点才需要 122px。

---

## 六、交互规则

1. **滚动淡入**：初始 opacity:0，滚动到可视区域后添加 `.visible` 类。两个元素参数**不同**：
   - `.entry` 文章卡片：translateY(24px)，1.2 秒
   - `.interlude` 穿插语：translateY(20px)，1.3 秒
2. **背景音乐**：右下角按钮控制，点击任何页面不中断（用 sessionStorage 记录播放状态）
3. **浮动导航** `.page-nav`：固定定位，**两个**按钮（回最新 `#latest` / 到底部 `#bottom`）；滚动 `scrollY > 560` 后出现（不是"超过一屏"）。位置随端变化：
   - 桌面端：**右侧垂直居中**（`top:50%; right:22px`，纵向排列）
   - 移动端：改为**右下角横排**（`bottom:18px`）
4. **菜单 hover**：下划线从中间展开动画
5. **标题 hover**：变砖红色

---

## 七、维护注意事项

### 之前维护混乱的根本原因

桌面端有**两套对齐中心**（页面中心 + 内容区中心），改的时候没分清哪个元素该用哪套，导致改了这里偏那里。

### 维护口诀

> **首屏/更多天数/收尾用页面中心，文章卡片/分隔节点用内容区中心。**

### 修改间距时

- 改桌面端间距时，检查移动端是否需要同步调整
- 改分隔节点间距时，确保上下均匀（padding-top = padding-bottom，margin-bottom 跟文章卡片的 margin-bottom 一致）

### 修改对齐时

- 桌面端内容区对齐用 `padding-left: 122px`，不要用 `transform: translateX`
- 移动端不需要任何水平偏移，全宽居中即可

### 文件结构

- `site.css` —— 所有样式（变量、基础、组件、移动端适配）
- `site.js` —— 交互逻辑（滚动淡入、音乐、导航、页面跳转等）
- `entries.js` —— **文章数据**（所有文章的标题、正文、日期、图片等，新增文章改这里）
- `memories.js` —— 回忆/往事数据
- `index.html` —— 首页（今时往日）
- `archive.html` —— 归档页（去看更多）
- `entry.html` —— 文章详情页模板
- `assets/img/` —— 所有图片资源
