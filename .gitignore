# ===== 百度同步助手：临时/冲突文件 =====
# 这些文件不该进 git，也不该被百度同步锁住 .git
*.bak
*.tmp
*~
*.swp
*.swo
*冲突*
*conflicted*
.baidu_sync*
同步助手*
# 百度同步在本地改文件名产生的副本
*.copy
*-副本

# ===== 操作系统 =====
.DS_Store
Thumbs.db
ehthumbs.db

# ===== 编辑器 / 工具链（如后续加 node 工具） =====
node_modules/
.vscode/
.idea/

# ===== 本地密钥 / 环境（服务器部署用，勿提交） =====
deploy/.env

# ============================================================
# 重要：.git 目录本身不要写进 .gitignore（它是仓库）。
# 但请在「百度同步」里把本仓库的 .git 目录排除同步，
# 避免百度同步在 git push/pull 时锁住 .git 导致 Permission denied。
# （你已确认 push/pull 正常，此条仅为防御性提醒）
# ============================================================
